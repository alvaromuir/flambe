//- nav
.pure-menu.pure-menu-open.pure-menu-horizontal.top-nav
  .container
    .pure-menu.pure-menu-open.pure-menu-horizontal.top-nav
      a.pure-menu-heading(href="/")
        img.hero(src="/assets/img/logo.png")
      ul
        li.active
          a(href="#") servings
        li
          a(href="#") chefs
        li
          a(href="#") food groups

      ul.nav-right
        li
          if everyauth.loggedIn
            - var greetingName = user.displayName.split(' ')[0]
            li
              .user-welcome
                .user-links
                  a.greeting(href="/chef/#{user.userName}") Welcome, #{greetingName}
                  a.logout(href="/logout") 
                    i.icon-remove-sign.pull-left 
                    span.text logout
                  .clearfix    
                a(href="/chef/#{user.userName}")
                  img(src="#{user.photoUrl}", alt="#{user.displayName}")
              
          else
            a(href="/login") sign in
        li
          form.pure-form
            .input-append
              span.add-on
                input.pure-u-1-1(type="search", placeholder="search flambé")
              i.icon-search
//- end nav